site:
    title: "碎文仓"
    subtitle: "一点点的积累也许就消失在那一刻"
    limit: 10
    theme: theme
    lang: zh
    url: "http://www.chole.io/blog"
    comment: username
    logo: "-/images/avatar.png"
    # link: "{category}/{year}/{month}/{day}/{title}.html"
    # root: "/blog"

authors:
    me:
        name: "Miba"
        intro: "“矮矬穷”&“高富帅”"
        avatar: "-/images/write.jpg"

build:
    # output: "public"
    port: 8000
    # Copied files to public folder when build
    copy:
        - "source/images"
    # Excuted command when use 'ink publish'
    publish: |
        git add . -A
        git commit -m "update"
        git push origin

